{
  "nodeId": "fix-reconcile",
  "desc": "Fix reconcile gap: missing = bn.consumes.filter(c => !fn.produces.includes(c))",
  "task": "Create decision document explaining the reconcile() gap.missing semantics fix",
  "consumes": [
    "src/protocol.ts",
    "tests/adv-reconcile.test.ts"
  ],
  "produces": [
    "docs/decisions/reconcile-gap.md"
  ],
  "keyFiles": [
    {
      "path": "src/protocol.ts",
      "lines": "171-197",
      "reason": "reconcile() implementation — the correct code already exists"
    },
    {
      "path": "tests/adv-reconcile.test.ts",
      "lines": "1-20",
      "reason": "Test comments document the bug and fix"
    }
  ],
  "pattern": "Decision document: Problem → Bug → Solution → Validation → Impact",
  "acceptance": [
    "Document explains the gap.missing semantics clearly",
    "Document shows the bug (symmetric difference) vs. correct (unmet demand only)",
    "Document includes all 8 test cases as validation",
    "Document explains why surplus produces must be excluded",
    "Document references implementation location and test coverage"
  ],
  "notes": "Code is already correct (reconcile() uses the right formula). This node documents the decision, not fixing broken code. See docs/decisions/orient-empty-produces.md for similar pattern."
}
